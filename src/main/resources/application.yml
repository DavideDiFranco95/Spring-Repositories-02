spring:
  datasource:
    url: jdbc:mysql://localhost:3306/programming_languagedb
    username: root
    password: Irisviel95
  jpa:
    database: mysql
    show_sql: true
    hibernate:
      ddl-auto: create-drop
  jackson:
    serialization:
      fail-on-empty-beans: false
server:
  port: 5050
